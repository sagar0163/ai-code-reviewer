# WAR ROOM PLAN

- [x] Add `timeout=10` to `requests.post` in `GitHubIntegration.create_review`.
- [x] Add exception handling for `requests.exceptions.Timeout` in `GitHubIntegration.create_review`.
- [x] Run tests to make sure everything passes.
