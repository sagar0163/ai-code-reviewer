# WAR ROOM PLAN

- [ ] Add `timeout=10` to `requests.post` in `GitHubIntegration.create_review`.
- [ ] Add exception handling for `requests.exceptions.Timeout` in `GitHubIntegration.create_review`.
- [ ] Run tests to make sure everything passes.
